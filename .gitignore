# Bulky / timestamped scan outputs — regenerated by the harness in CI.
# Keep the directory but not the raw runs. Scored metrics can be committed
# explicitly as evidence when needed.
results/zap/*/
results/zap/*-latest
*.pyc
__pycache__/
.env
.venv/
venv/
